# --- Appointment requests ---
# Optional. When set, every appointment request is POSTed as JSON to this URL
# (Zapier / Make / a Slack incoming webhook / your own endpoint). When unset,
# requests are validated and logged to the server console only.
APPOINTMENT_WEBHOOK_URL=
